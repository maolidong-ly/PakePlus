# Guide & Instructions / 说明与操作指南

In-app copy: `helpGuide.js` (zh / en). Open via header **说明 / Help**. No product name; no school/institution framing.

Packaged persistence: class / teacher / room / course / timetable / period-template data are stored in `schedule_app_data.json` under the app data directory (see `localStore.js`).
